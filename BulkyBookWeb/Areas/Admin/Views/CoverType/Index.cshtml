@model IEnumerable<CoverType>
@{
    ViewData["Title"] = "Index";
}
<partial name="_Notification"/>
<div class="container p-3">
   
    <div class="row pt-4">
    <div class="col-6">
        <h2 class="text-primary">Cover Types List</h2>
    </div>

     <div class="col-6 text-end" >
            <a asp-controller="CoverType" asp-action="Create" class="btn btn-primary"> <i class="bi bi-plus-circle"></i> Create New Cover Type</a>
     </div>
        
    </div>

</div>
<table class="table table-boadered table-striped" style="width:100%">
    <thead>
        <tr>
            <th>
                Cover Type
            </th>
            
            <th>
                Action
            </th>

        </tr>

    </thead>
    <tbody>
        @foreach(var obj in Model)
        {
            <tr>
                <td width="40%">
                    @obj.Name
                </td>
                @*<td width="40">
                    @obj.DisplayOrder
                    
                </td>*@
                @*<div class="col-6 text-end">
                    <a asp-controller="Category" asp-action="Detail" class="btn btn-primary"> <i class="bi bi-plus-circle"></i> Create New Category</a>
                </div>*@
                <td>
                 @*  <div class=" text-end">
                    <a asp-controller="Category" asp-action="Edit" class="btn btn-primary"> Edit</a>
                </div>
                    <div class=" text-end">
                    <a asp-controller="Category" asp-action="Details" class="btn btn-primary"> Detail</a>
                </div>
                 <div class=" text-end">
                    <a asp-controller="Category" asp-action="Delete" class="btn btn-primary"> Delete</a>
                 </div>*@
                
                    <a asp-action="Edit" asp-route-id="@obj.Id">Edit</a> |
                    <a asp-action="Details" asp-route-id="@obj.Id">Details</a> |
                    <a asp-action="Delete" asp-route-id="@obj.Id">Delete</a>
                </td>
                

            </tr>
        }

    </tbody>
</table>

